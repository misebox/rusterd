{
  "name": "rusterd-demo",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "load": "mkdir -p pkg && cp ../pkg/* pkg/",
    "dev": "bun run load; vite",
    "build": "bun run load && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "solid-js": "^1.9.0"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vite-plugin-solid": "^2.11.0"
  }
}
